name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:

    runs-on: ubuntu-latest

    steps:

    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ anas444nn }}
        password: ${{ dckr_pat_Zr71G7qtmaDRbj_0qqgUDrSIGfg }}

    - name: Build Docker Image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/calculator-app:latest .

    - name: Run Container Test
      run: docker run ${{ secrets.DOCKER_USERNAME }}/calculator-app:latest

    - name: Push Image to Docker Hub
      run: docker push ${{ secrets.DOCKER_USERNAME }}/calculator-app:latest